# ViboCodium
An IDE for iOS devices: a native Swift + Metal editor, JIT via StikDebug,
and sideload signing via SideStore's pipeline.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the component breakdown and
build-system decisions. Run `make bootstrap` after cloning to pull in the
vendored submodules.
